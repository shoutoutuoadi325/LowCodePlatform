# 曦源IoT低代码平台前端技术架构文档

## 1. 架构设计

```mermaid
graph TD
    A[用户浏览器] --> B[Vue 3 前端应用]
    B --> C[Vue Router 路由]
    B --> D[Pinia 状态管理]
    B --> E[Axios HTTP客户端]
    E --> F[设备服务 API :8081]
    E --> G[场景服务 API :8082]
    E --> H[设备类型服务 API :8081]
    B --> I[WebSocket 连接]
    I --> J[实时数据推送]

    subgraph "前端层"
        B
        C
        D
        E
        I
    end

    subgraph "后端服务层"
        F
        G
        H
        J
    end

    subgraph "组件库"
        K[Element Plus UI]
        L[ECharts 图表]
        M[Vue Flow 流程图]
        N[自定义组件]
    end

    B --> K
    B --> L
    B --> M
    B --> N
```

## 2. 技术描述

- 前端：Vue@3 + Element Plus@2 + Vue Router@4 + Pinia@2 + Axios@1 + ECharts@5 + Vue Flow@1
- 构建工具：Vite@4
- 样式：CSS3 + Element Plus主题
- 实时通信：WebSocket + MQTT over WebSocket

## 3. 路由定义

| 路由 | 用途 |
|------|------|
| /dashboard | 仪表板，显示系统概览和快速操作 |
| /devices | 设备管理，设备列表和操作 |
| /device-types | 设备类型管理，类型定义和编辑 |
| /device-monitor | 设备监控，实时状态和历史数据 |
| /scenes | 场景管理，场景列表和操作 |
| /scene-designer | 场景编排，可视化场景设计器 |
| /system-config | 系统配置，MQTT和系统参数配置 |
| /data-analysis | 数据分析，统计图表和报表 |
| /simulator-management | 模拟器管理，模拟器状态和配置 |
| /login | 登录页面，用户身份验证 |

## 4. API定义

### 4.1 设备类型管理API

获取设备类型列表
```
GET /api/device-types
```

Response:
| 参数名称 | 参数类型 | 描述 |
|----------|----------|------|
| id | string | 设备类型ID |
| name | string | 设备类型名称 |
| identifier | string | 设备类型标识符 |
| description | string | 描述 |
| properties | array | 属性定义列表 |
| operations | array | 操作定义列表 |
| events | array | 事件定义列表 |

创建设备类型
```
POST /api/device-types
```

Request:
| 参数名称 | 参数类型 | 是否必需 | 描述 |
|----------|----------|----------|------|
| name | string | true | 设备类型名称 |
| identifier | string | true | 设备类型标识符 |
| description | string | false | 描述 |
| properties | array | false | 属性定义列表 |
| operations | array | false | 操作定义列表 |
| events | array | false | 事件定义列表 |

### 4.2 设备监控API

获取设备实时状态
```
GET /api/devices/{deviceId}/state
```

获取设备历史数据
```
GET /api/devices/{deviceId}/history?startTime={start}&endTime={end}
```

### 4.3 WebSocket API

实时数据推送
```
WebSocket: ws://localhost:8080/ws/device-data
```

消息格式:
```json
{
  "type": "device_state_update",
  "deviceId": "device_001",
  "timestamp": "2024-01-01T00:00:00Z",
  "data": {
    "temperature": 25.5,
    "humidity": 60.2,
    "status": "online"
  }
}
```

## 5. 组件架构

### 5.1 组件层次结构

```mermaid
graph TD
    A[App.vue] --> B[Layout组件]
    B --> C[Header导航]
    B --> D[Sidebar侧边栏]
    B --> E[Main内容区]
    E --> F[页面组件]
    F --> G[业务组件]
    G --> H[基础组件]

    subgraph "页面组件"
        F1[DeviceTypes.vue]
        F2[DeviceMonitor.vue]
        F3[SystemConfig.vue]
        F4[DataAnalysis.vue]
    end

    subgraph "业务组件"
        G1[DeviceTypeEditor.vue]
        G2[DeviceStatusCard.vue]
        G3[DataChart.vue]
        G4[PropertyEditor.vue]
    end

    subgraph "基础组件"
        H1[JsonEditor.vue]
        H2[StatusIndicator.vue]
        H3[DataTable.vue]
        H4[FormBuilder.vue]
    end

    F --> F1
    F --> F2
    F --> F3
    F --> F4
    G --> G1
    G --> G2
    G --> G3
    G --> G4
    H --> H1
    H --> H2
    H --> H3
    H --> H4
```

### 5.2 状态管理

```mermaid
graph TD
    A[Pinia Store] --> B[Device Store]
    A --> C[DeviceType Store]
    A --> D[Scene Store]
    A --> E[System Store]
    A --> F[User Store]

    B --> B1[设备列表]
    B --> B2[设备状态]
    B --> B3[实时数据]

    C --> C1[设备类型列表]
    C --> C2[当前编辑类型]
    C --> C3[类型模板]

    D --> D1[场景列表]
    D --> D2[活跃场景]

    E --> E1[系统配置]
    E --> E2[MQTT状态]

    F --> F1[用户信息]
    F --> F2[权限]
```

## 6. 数据模型

### 6.1 设备类型模型

```mermaid
erDiagram
    DEVICE_TYPE ||--o{ PROPERTY_DEFINITION : has
    DEVICE_TYPE ||--o{ OPERATION_DEFINITION : has
    DEVICE_TYPE ||--o{ EVENT_DEFINITION : has
    DEVICE ||--|| DEVICE_TYPE : belongs_to

    DEVICE_TYPE {
        string id PK
        string name
        string identifier
        string description
        timestamp created_at
        timestamp updated_at
    }

    PROPERTY_DEFINITION {
        string id PK
        string device_type_id FK
        string name
        string identifier
        string data_type
        boolean required
        string default_value
        string description
    }

    OPERATION_DEFINITION {
        string id PK
        string device_type_id FK
        string name
        string identifier
        string description
        json parameters
    }

    EVENT_DEFINITION {
        string id PK
        string device_type_id FK
        string name
        string identifier
        string description
        json properties
    }

    DEVICE {
        string id PK
        string device_type_id FK
        string name
        string location
        string room
        string status
        json properties
        timestamp created_at
        timestamp updated_at
    }
```

### 6.2 前端数据结构

设备类型TypeScript接口定义：

```typescript
interface DeviceType {
  id: string;
  name: string;
  identifier: string;
  description?: string;
  properties: PropertyDefinition[];
  operations: OperationDefinition[];
  events: EventDefinition[];
  createdAt: string;
  updatedAt: string;
}

interface PropertyDefinition {
  id: string;
  name: string;
  identifier: string;
  dataType: 'string' | 'number' | 'boolean' | 'object';
  required: boolean;
  defaultValue?: any;
  description?: string;
}

interface OperationDefinition {
  id: string;
  name: string;
  identifier: string;
  description?: string;
  parameters: ParameterDefinition[];
}

interface EventDefinition {
  id: string;
  name: string;
  identifier: string;
  description?: string;
  properties: PropertyDefinition[];
}

interface ParameterDefinition {
  name: string;
  dataType: string;
  required: boolean;
  description?: string;
}
```